.idea
*.iml

# Secrets: never commit
.env
.env.*
!.env.example

# Dependencies
node_modules/

# Build output
dist/
.next/
.next-dev/
out/

# Local scratch
tmp/
.vercel

# OS
.DS_Store

# Build artifacts from the verification pipeline
verification-report.json
test-report.json
sbom.cdx.json
*.tsbuildinfo
packages/ui/dist-app/

# The encrypted wallet written by a local `make dev`. It is sealed, but a
# committed wallet is still a wallet in a public repository, and the manifest
# covers packages/ and spec/ rather than this.
.nullroute-store/

# Synthetic images from `make fixture-image`. Half a gigabyte of mostly zeroes,
# and regenerated deterministically by the tool, so there is nothing to keep.
.fixture/
